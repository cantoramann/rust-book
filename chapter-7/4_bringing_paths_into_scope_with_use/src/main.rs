use std::collections::HashMap;
use std::collections::*;
use std::fmt;
use std::io::Result as IoResult;
use std::io::{self, Write}; // take io as io::, and io::Write as Write // glob op

fn main() {
    let mut map = HashMap::new();
    map.insert(1, 2);
}
